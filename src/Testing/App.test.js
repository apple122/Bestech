import { render } from "@testing-library/react"
import Period_type from "../components/period_type/period_type/Index"

function testing(a, b) {
    return a - b
}
test('should first', () => {
    render (<Period_type />)

    expect()
})
